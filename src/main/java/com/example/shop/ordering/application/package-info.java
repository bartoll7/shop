/**
 * ORDERING — application layer.
 *
 * <p>Application Services orchestrate use cases (e.g. "place an order").
 * They coordinate the domain but contain NO business rules themselves.
 * Allowed to know Spring. Weeks 9-10.
 */
package com.example.shop.ordering.application;
