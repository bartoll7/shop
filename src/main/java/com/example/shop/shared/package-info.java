/**
 * SHARED kernel — concepts deliberately shared across subdomains.
 *
 * <p>Keep this SMALL. Shared Kernel is a strategic pattern (week 11) with a
 * cost: every subdomain becomes coupled to whatever lives here. Money is the
 * usual justified candidate. When in doubt, do NOT put it here.
 */
package com.example.shop.shared;
